package core

// Shared option slices used across multiple editor field definitions.

// OptionsOffOn is the canonical boolean select for fields that default to disabled.
var OptionsOffOn = []string{"false", "true"}

// OptionsOnOff is the canonical boolean select for fields that default to enabled.
var OptionsOnOff = []string{"true", "false"}
